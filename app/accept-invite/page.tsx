"use client";
import { useEffect,useState } from "react";
import { Button } from "@/components/ui/button";
export default function AcceptInvitePage(){const [token,setToken]=useState(""),[state,setState]=useState<"loading"|"signin"|"done"|"error">("loading"),[message,setMessage]=useState("Checking your invitation…"),[workspace,setWorkspace]=useState("");useEffect(()=>{const value=new URLSearchParams(window.location.search).get("token")||"";setToken(value);if(!value){setState("error");setMessage("This invitation link is incomplete.");return;}void fetch("/api/invitations",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({token:value})}).then(async response=>{let data:{error?:string;workspace_id?:string;role?:string}={};try{data=await response.json();}catch{}if(response.status===401){setState("signin");setMessage("Sign in with the exact email address that was invited, then open this link again.");return;}if(!response.ok)throw new Error(data.error||"This invitation could not be accepted.");setWorkspace(data.workspace_id||"");setState("done");setMessage(`Invitation accepted${data.role?` as ${data.role}`:""}.`);}).catch(error=>{setState("error");setMessage(error instanceof Error?error.message:"This invitation could not be accepted.");});},[]);const returnTo="/accept-invite?token="+encodeURIComponent(token);return <main style={{maxWidth:680,margin:"64px auto",padding:24,fontFamily:"system-ui",lineHeight:1.55}}><meta name="referrer" content="no-referrer"/><div style={{fontSize:13,fontWeight:800,letterSpacing:1}}>SCOPELEDGER</div><h1 style={{fontSize:34,margin:"8px 0 10px"}}>Workspace invitation</h1><div role={state==="error"?"alert":"status"} style={{padding:20,border:"1px solid #dfe3ea",borderRadius:14,marginTop:20}}><p style={{marginTop:0}}>{message}</p>{state==="signin"&&<a href={"/signin-with-chatgpt?return_to="+encodeURIComponent(returnTo)} target="_top"><Button>Sign in with invited email</Button></a>}{state==="done"&&<a href={workspace?"/?workspace="+encodeURIComponent(workspace):"/"} target="_top"><Button>Open ScopeLedger</Button></a>}{state==="loading"&&<p style={{color:"#666"}}>Please keep this tab open.</p>}</div><p style={{color:"#666",marginTop:20}}>Invitation links expire and can be revoked. A link does not bypass identity checks: the signed-in email must match the invited address.</p></main>}
